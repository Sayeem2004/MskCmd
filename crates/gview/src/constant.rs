/**
 * This is the default weight of a node in the graph. The unit is kilobytes.
 */
pub const WEIGHT: f64 = 1.0;

/**
 * This is the default exponent used to calculate the radius of a vertex.
 */
pub const EXP: f64 = 1.0 / 3.0;

/**
 * This is the default delimiter used to separate the nodes in the graph.
 */
pub const DELIM: &str = " ";
